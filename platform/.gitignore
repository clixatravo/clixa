node_modules/
.next/
out/
build/
dist/

.env
.env.*
!.env.example

.DS_Store
*.pem
*.log
npm-debug.log*
.vercel
next-env.d.ts
tsconfig.tsbuildinfo
.env*

# Médias versés localement (production : Cloudflare R2)
public/medias/
catalogue-reel.json
test-results/
playwright-report/
.playwright/
storybook-static/
apercus/

# Réglage local du serveur de développement (panneau navigateur)
.claude/

# Le spécimen du certificat est fabriqué à la demande, jamais suivi.
#   npx payload run scripts/specimen-certificat.tsx [slug]
# Un exemplaire déposé en dur vieillit au premier changement du document réel :
# c'est exactement ce qui était arrivé au spécimen refait à la main, qui dormait
# ici sans lien depuis aucune page.
public/images/marketing/certificat-specimen.*
